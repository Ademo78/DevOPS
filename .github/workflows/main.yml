name: CI DevOps TP
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Hello DevOps
        run: echo "Le pipeline fonctionne pour Ademo78 !"