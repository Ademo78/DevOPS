- name: Run Proxy
  docker_container:
    name: my-proxy
    image: ademo78711/my-httpd:1.0
    ports:
      - "80:80"
    networks:
      - name: app-network
  vars:
    ansible_python_interpreter: /opt/docker_venv/bin/python
